$( document ).ready(function() {
    var socket = io("/index");
    
    so
    
    $("#quickSessionButton").click(function(){
        if($("#linkInput").val() != "")
        {
            alert("there's something here");
        }
    });
});

